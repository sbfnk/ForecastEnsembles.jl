import{_ as o,o as a,c as t,an as r}from"./chunks/framework.6kDUSiVv.js";const g=JSON.parse('{"title":"Roadmap","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":null}'),s={name:"roadmap.md"};function i(n,e,c,l,d,h){return a(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="Roadmap" tabindex="-1">Roadmap <a class="header-anchor" href="#Roadmap" aria-label="Permalink to &quot;Roadmap {#Roadmap}&quot;">​</a></h1><p>Planned work, organised around the two axes (combination operations and weighting schemes) plus the workflow and ecosystem pieces. Cross-references to the issues that prompted items are in parentheses.</p><h2 id="Weighting-schemes-axis-2" tabindex="-1">Weighting schemes (axis 2) <a class="header-anchor" href="#Weighting-schemes-axis-2" aria-label="Permalink to &quot;Weighting schemes (axis 2) {#Weighting-schemes-axis-2}&quot;">​</a></h2><p>The organising idea is <strong>stacking with any target</strong>: choose weights to minimise a proper score of the <em>combined</em> forecast on past observations. The built foundation — <code>CRPSStacking</code>/<code>QRA</code> (closed-form CRPS/WIS), generic <code>Stacking{Score}</code>, <code>InverseScore</code>, <code>Hedge</code>, <code>PartialPooling</code>, and the <code>Windowed</code> wrapper — leaves these still to do:</p><ul><li><strong>Log-score stacking</strong> and <strong>BMA.</strong> BMA is essentially log-score stacking of a mixture fitted by EM, so it folds into the generic stacker rather than standing alone. (Log score of a sample mixture needs a density estimate, so this is a follow-up to the sample-CRPS path now in place.)</li></ul><h2 id="Workflow:-choosing-a-scheme" tabindex="-1">Workflow: choosing a scheme <a class="header-anchor" href="#Workflow:-choosing-a-scheme" aria-label="Permalink to &quot;Workflow: choosing a scheme {#Workflow:-choosing-a-scheme}&quot;">​</a></h2><ul><li><strong>Calibration diagnostics</strong> — PIT histograms, coverage, pairwise model comparison — are general forecast-evaluation tools, not ensemble-specific, so they belong in the scoring/evaluation layer (<a href="https://github.com/EpiAware/ProjectProposals/issues/2" target="_blank" rel="noreferrer">ForecastScoring.jl, ProjectProposals#2</a>), which we consume rather than reimplement.</li></ul><h2 id="Recalibration" tabindex="-1">Recalibration <a class="header-anchor" href="#Recalibration" aria-label="Permalink to &quot;Recalibration {#Recalibration}&quot;">​</a></h2><p>A separate pipeline stage — it transforms a single predictive distribution rather than aggregating several — so it is out of scope for the core combine story, but planned. A <code>Recalibrator</code> abstract type with <code>fit(m, training_forecasts, observations)</code> and <code>recalibrate(ft, fitted)</code>.</p><p><a href="https://lipiecki.github.io/PostForecasts.jl/stable/" target="_blank" rel="noreferrer">PostForecasts.jl</a> implements five methods (<code>Normal</code>, <code>CP</code>, <code>IDR</code>, <code>QR</code>, <code>LassoQR</code>), but they all take point forecasts as input and return quantile forecasts — point in, distribution out. Recalibrating an ensemble&#39;s quantile output is the other direction, so there is no off-the-shelf fit; three paths, in increasing effort:</p><ul><li><p><em>Contribute to PostForecasts.jl</em> — its IDR could generalise to richer input (Henzi/Ziegel/Gneiting&#39;s IDR is distributional regression on arbitrary covariates, not point-only). A PR benefits everyone.</p></li><li><p><em>Sibling package</em> over PostForecasts.jl + ours.</p></li><li><p><em>Implement here</em> — IDR has a clean R reference in <a href="https://github.com/AlexanderHenzi/isodistrreg" target="_blank" rel="noreferrer">isodistrreg</a>.</p></li></ul><p>Candidate methods: <strong>spread / dispersion adjustment</strong> (a one-parameter widen/narrow of the combined forecast around its median to hit nominal coverage — the simplest parametric <code>Recalibrator</code>, and distinct from PostForecasts.jl&#39;s five methods, which map <em>point</em> forecasts to quantiles rather than rescaling an existing distribution); <strong>empirical PIT mapping</strong> (also no upstream dependency); <strong>IDR</strong> (Henzi, Ziegel, Gneiting 2019, <a href="https://arxiv.org/abs/1909.03725" target="_blank" rel="noreferrer">arXiv:1909.03725</a>); <strong>CRPS-minimising parametric recalibration</strong> (the <code>CRPSStacking</code> machinery applied one model at a time); <strong>BLP</strong>.</p><h2 id="Ecosystem" tabindex="-1">Ecosystem <a class="header-anchor" href="#Ecosystem" aria-label="Permalink to &quot;Ecosystem {#Ecosystem}&quot;">​</a></h2><ul><li><p><strong>Shared forecast types</strong> — an array-backed <code>Forecast</code>/<code>ForecastSet</code> layer (<code>ProbabilisticForecasts.jl</code>) that ForecastBaselines.jl, PostForecasts.jl, ForecastEnsembles.jl and the scoring stack all speak. The real unifier; see <code>design/forecast-types.md</code>.</p></li><li><p><strong>Evaluation</strong> — scoring lives in ScoringRules.jl / ForecastScoring.jl (<a href="https://github.com/EpiAware/ProjectProposals/issues/2" target="_blank" rel="noreferrer">#2</a>); we consume it, we do not build it.</p></li><li><p><strong>A domain-agnostic forecasting org</strong> — a possible neutral home grouping ForecastBaselines.jl, ForecastEnsembles.jl, ScoringRules.jl and potentially non-epi packages (PostForecasts.jl). Under discussion; packages are built to be org-portable regardless.</p></li></ul><h2 id="Infrastructure" tabindex="-1">Infrastructure <a class="header-anchor" href="#Infrastructure" aria-label="Permalink to &quot;Infrastructure {#Infrastructure}&quot;">​</a></h2><ul><li><p><strong>hubverse directory IO</strong> — read a <code>model-output/</code> tree and write a valid submission directly.</p></li><li><p><strong>Benchmarks.</strong> A <code>benchmark/</code> suite times the headline combine operations and weight estimators on a realistic <code>model_out_tbl</code> (<code>julia --project=benchmark benchmark/run.jl</code>). Still to do: wire it into the docs performance-history page (needs a benchmark CI job), and a head-to-head timing comparison against the R packages.</p></li></ul><h2 id="Out-of-scope-for-now" tabindex="-1">Out of scope (for now) <a class="header-anchor" href="#Out-of-scope-for-now" aria-label="Permalink to &quot;Out of scope (for now) {#Out-of-scope-for-now}&quot;">​</a></h2><ul><li><p>Decision-theoretic combination (utility-weighted ensembles).</p></li><li><p>A GUI.</p></li></ul>',18)])])}const m=o(s,[["render",i]]);export{g as __pageData,m as default};
